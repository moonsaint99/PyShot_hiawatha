import numpy as np
import pickfile as pf

# We will define a function here that can assimilate all of the pickdata from a shot record,
# all collected into one folder. This function will return a list of Pickfile objects.

